<!-- app/views/products/_product.html.erb -->
<!-- <div class="product">
  
  <p><strong>Description:</strong> <%= product.description %></p>
  <p><strong>Brand:</strong> <%= product.brand %></p>
  
  <p><strong>Price:</strong> <%= number_to_currency(product.price) %></p>
  <p><strong>Total Stock Amount:</strong> <%= product.total_stock_amount %></p>

  
</div>
-->
<style>
    .image {
      width: 100%;
      min-height:250px;
      max-height: 300px;
      justify-content: center;
    }
</style>




    <div class="product-card">
        <div class="card-body">
            <h5 class="card-title"><%= product.name %></h5>
            <% if product.image.attached? %>
                <%= image_tag product.image, class: "image" %> <!-- Assuming you have a method like "url" in your Image model -->
            <% else %>
                <!-- Handle the case where there is no image -->
                <p>No image available</p>
            <% end %>
            <p class="card-text"><strong>Price:</strong> <%= number_to_currency(product.price) %></p>
        </div>

    </div>

